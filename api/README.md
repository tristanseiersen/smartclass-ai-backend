SmartClass AI custom backend (Vercel)

ENV:
- OPENAI_API_KEY=sk_...

Deploy:
- Create Git repo, push to GitHub.
- Connect repo to Vercel and deploy.
- Set OPENAI_API_KEY in Vercel dashboard (Project > Settings > Environment Variables).
